# Supabase

NEXT_PUBLIC_SUPABASE_URL=https://hqvyjugbplxsgkecybgc.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_EyO6WcxG7uXYFOwPKyqWyw_DLkoWqN3

SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google Maps (for admin panel map views)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-key

# Firebase Cloud Messaging (for push notifications)
# Two separate Firebase projects: one for driver app, one for rider app

# Driver App Firebase Configuration
# Option 1: Provide service account JSON as a string (recommended for production)
FIREBASE_DRIVER_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"driver-project-id",...}

# Option 2: Provide path to service account JSON file (alternative)
# FIREBASE_DRIVER_SERVICE_ACCOUNT_PATH=/path/to/driver-service-account-key.json

# Rider App Firebase Configuration
# Option 1: Provide service account JSON as a string (recommended for production)
FIREBASE_RIDER_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"rider-project-id",...}

# Option 2: Provide path to service account JSON file (alternative)
# FIREBASE_RIDER_SERVICE_ACCOUNT_PATH=/path/to/rider-service-account-key.json
